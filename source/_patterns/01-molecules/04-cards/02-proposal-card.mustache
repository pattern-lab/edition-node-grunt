<div class="panel">
  <div class="main">
    <p>In the <a href="{{ url }}">$Working-group-name</a> in the <a href="{{ url }}">$Campaign-name Campaign</a></p>
    <h1 class="heading--title">{{ title.short }}</h1>
    <p>{{ excerpt.medium }}</p>
    {{> molecules-tags }}
  </div>
  <div class="action">
    {{> molecules-voting-module }}
  </div>
</div>
