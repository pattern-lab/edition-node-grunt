<header>
  <p class="heading--subheader">$Assembly</p>
  <h1 class="heading--display">{{ title.short }}</h1>
  <p heading--headline>{{ excerpt.medium }}</p>
  <p>In the <a href="{{ url }}" class="b-inner">$Working-group-name</a> in the <a href="{{ url }}" class="b-inner">$Campaign-name Campaign</a></p>
  {{> molecules-tags }}
</header>
