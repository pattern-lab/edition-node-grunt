'use strict';

module.exports = function(grunt) {

  grunt.initConfig({
    patternlab: {
        options: {
          // You can overwrite default config here
        }
    }
  });

  grunt.loadTasks('tasks');
};
